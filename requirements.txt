# FlowShift Python dependencies
#
# Pillow provides animated clipboard preview decoding. The clipboard runtime
# degrades safely without it, but installed FlowShift includes the feature.
Pillow>=10,<13

# The isolated overlay host embeds the React/Vite shell through Windows
# WebView2. Version 5.4 supports the Python versions used by the installer.
pywebview==5.4
